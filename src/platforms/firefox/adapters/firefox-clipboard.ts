/**
 * Firefox clipboard adapter - proxy re-export of Chrome adapter.
 * Chrome adapter uses standard navigator.clipboard API which works identically in Firefox.
 */
export * from '../../chrome/adapters/chrome-clipboard.js';
