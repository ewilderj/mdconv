/**
 * Firefox DOM parser adapter - proxy re-export of Chrome adapter.
 * Chrome adapter uses standard DOMParser API which works identically in Firefox.
 */
export * from '../../chrome/adapters/chrome-dom-parser.js';
